__pycache__/
todo.md

.qodo
.agent
.cursor
.claude

.media-tools

benchmark.*
*.log
*.resolved
*.out
.env

testing/
output/

.history/
.translate_cache.json
.translate_cache.json.tmp
benchmark-results

findings.md
task_plan.md
progress.md
docs/

# Plugin-installed skills (e.g. codex symlinks)
superpowers/
# Symlink into the shared ~/.agents/skills store; the target is an
# absolute machine-local path, so the link cannot be shared.
frontend-design

# Installed by `npm install` inside md-publish; the lockfile is tracked.
node_modules/
